# GitHub API Token (Optional, but recommended to avoid rate limits)
# Create one at: https://github.com/settings/tokens
# No scopes needed for public repo content
GITHUB_TOKEN=

# Email Configuration
EMAIL_USER=
EMAIL_PASS=
